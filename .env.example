# Copy to .env and fill in
OPENAI_API_KEY=sk-...
# Default model used by the analyzer. Cheap + fast + strong enough for sentiment.
REVREPLY_MODEL=openai/gpt-4o-mini
# Path of compiled (optimized) DSPy program; loaded at API startup if it exists.
REVREPLY_COMPILED_PATH=compiled/analyzer.json
